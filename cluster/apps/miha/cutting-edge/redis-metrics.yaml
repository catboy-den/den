apiVersion: v1
kind: Service
metadata:
  name: redis-metrics-service
spec:
  selector:
    component: redis-cutting-edge-metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-metrics
spec:
  selector:
    matchLabels:
      component: redis-cutting-edge-metrics
      stack: cutting-edge
