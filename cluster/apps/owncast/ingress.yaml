apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: owncast
spec:
  rules:
  - host: stream.catboy.rest
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: owncast
            port:
              number: 10088
