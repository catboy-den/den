apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: vault-secrets-webhook
  namespace: vault
spec:
  interval: 30m
  chart:
    spec:
      chart: vault-secrets-webhook
      version: "1.x"
      sourceRef:
        kind: HelmRepository
        name: bank-vaults-repo
        namespace: vault
      interval: 12h
  # https://github.com/banzaicloud/bank-vaults/blob/main/charts/vault-secrets-webhook/values.yaml
  values:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
