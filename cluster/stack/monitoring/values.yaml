apiVersion: v1
kind: Secret
metadata:
  name: kube-promotheus-stack-values
  annotations:
    vault.security.banzaicloud.io/vault-addr: "https://vault.vault:8200"
    vault.security.banzaicloud.io/vault-role: "default"
    vault.security.banzaicloud.io/vault-tls-secret: vault-tls
# https://github.com/prometheus-community/helm-charts/blob/main/charts/kube-prometheus-stack/values.yaml
stringData:
  values: |-
    alertmanager:
      enabled: false
    prometheus:
      prometheusSpec:
        retention: 24h
        resources:
          requests:
            cpu: 200m
            memory: 200Mi
        podMonitorNamespaceSelector: {}
        podMonitorSelector:
          matchLabels:
            app.kubernetes.io/component: monitoring
    grafana:
      ingress:
        enabled: true
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-issuer
          traefik.ingress.kubernetes.io/router.entrypoints: websecure
          traefik.ingress.kubernetes.io/router.tls: "true"
        hosts:
          - grafana.catboy.rest
        tls:
          - secretName: grafana-cert
            hosts:
              - grafana.catboy.rest
      grafana.ini:
        server:
          domain: grafana.catboy.rest
          root_url: https://grafana.catboy.rest
        auth:
          disable_login_form: true
        auth.basic:
          enabled: false
        auth.generic_oauth:
          name: Dex
          enabled: true
          auto_login: true
          scopes: openid profile email groups
          groups_attribute_path: info.groups
          role_attribute_path: '{"catboy-den:cluster-admin": ["Admin"], "catboy-den:cluster-user": ["Viewer"]}'
          client_id: grafana
          client_secret: ${vault:secret/data/dex-staticClient-secret#secret}
          auth_url: https://dex.catboy.rest/auth
          token_url: https://dex.catboy.rest/token
          api_url: https://dex.catboy.rest/userinfo
